# DB Observe Scripts

This folder can hold helper scripts for DB observation.
Current workflow in `.github/workflows/task-service-ci-cd.yml` executes inline `psql` queries,
so this folder is optional. Add custom queries or helpers here as needed.
