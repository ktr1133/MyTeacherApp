# スケジュールタスク管理機能 要件定義書（モバイル版）

## 更新履歴

| 日付 | 更新者 | 更新内容 |
|------|--------|---------|
| 2025-12-08 | GitHub Copilot | 初版作成: Phase 2.B-7実装に向けたモバイル版要件定義 |

---

## 1. 概要

MyTeacher モバイルアプリにおけるスケジュールタスク管理機能の要件定義書です。グループメンバーに対して、定期的に自動でタスクを作成する機能を提供します。

### Web版との差異

| 項目 | Web版 | モバイル版 |
|------|-------|-----------|
| スケジュール一覧 | ✅ 実装済み | ❌ 未実装（Phase 2.B-7で実装） |
| スケジュール作成 | ✅ 実装済み | ❌ 未実装（Phase 2.B-7で実装） |
| スケジュール編集 | ✅ 実装済み | ❌ 未実装（Phase 2.B-7で実装） |
| スケジュール削除 | ✅ 実装済み | ❌ 未実装（Phase 2.B-7で実装） |
| 一時停止・再開 | ✅ 実装済み | ❌ 未実装（Phase 2.B-7で実装） |
| 実行履歴表示 | ✅ 実装済み | ❌ 未実装（Phase 2.B-7で実装） |
| ランダム割り当て | ❌ 未実装（将来拡張用） | ❌ 未実装（Phase 2.B-7では実装しない） |

---

## 2. 機能要件

### 2.1 スケジュールタスク一覧表示

#### 2.1.1 一覧表示

```
ホーム画面 or 設定画面
    ↓
「スケジュールタスク」ボタンタップ
    ↓
ScheduledTaskListScreen表示
    ↓
スケジュールタスク一覧表示（カード形式）
```

**表示内容**:
- タイトル
- 説明
- 実行スケジュール（日次・週次・月次）
- ステータス（有効・一時停止）
- 担当者（特定ユーザー or グループ全員）
- 報酬トークン数
- 期限設定（作成後X日/X時間）
- 実行期間（開始日～終了日）
- タグ

#### 2.1.2 ステータス表示

**有効状態**: 
- ✅ 緑色のバッジ「有効」
- スケジュール通りに実行中

**一時停止状態**:
- ⏸️ グレー色のバッジ「一時停止」
- スケジュール実行を停止中

#### 2.1.3 空状態

スケジュールタスクが0件の場合：
```
┌─────────────────────────────────┐
│                                 │
│    📅 スケジュールタスクなし     │
│                                 │
│ 定期的に自動実行するタスクを    │
│ 設定できます。                   │
│                                 │
│    [スケジュールを作成]          │
│                                 │
└─────────────────────────────────┘
```

### 2.2 スケジュールタスク作成

#### 2.2.1 作成フロー

```
ScheduledTaskListScreen
    ↓
「作成」ボタンタップ
    ↓
ScheduledTaskCreateScreen表示
    ↓
必要情報入力
├ 基本情報（タイトル、説明）
├ タスク設定（画像必須、承認必須、報酬）
├ 担当者設定（特定ユーザー or グループ全員）
├ スケジュール設定（日次・週次・月次、複数可）
├ 期限設定（日数・時間）
├ 実行期間（開始日、終了日）
├ その他設定（祝日スキップ、翌営業日実行、前回未完了削除）
└ タグ
    ↓
「作成」ボタンタップ
    ↓
API呼び出し（POST /api/scheduled-tasks）
    ↓
成功時にScheduledTaskListScreenへ戻る
```

#### 2.2.2 入力項目詳細

**基本情報**:
- タイトル: 必須、最大255文字
- 説明: 任意、最大5000文字

**タスク設定**:
- 画像添付必須: チェックボックス
- 承認必須: チェックボックス
- 報酬トークン: 数値入力（0～999999）

**担当者設定**:
- ランダム割り当て: ❌ **Phase 2.B-7では実装しない**
- 特定ユーザー指定: ドロップダウン選択
- 未指定の場合: グループの編集権限なしメンバー全員

**スケジュール設定（複数設定可）**:

1. **日次スケジュール**
   - 時刻入力（HH:MM形式）
   - 例: 「毎日 09:00」

2. **週次スケジュール**
   - 時刻入力（HH:MM形式）
   - 曜日選択（複数選択可）
     - 日曜（0）、月曜（1）、火曜（2）、水曜（3）、木曜（4）、金曜（5）、土曜（6）
   - 例: 「毎週 月・水・金 09:00」

3. **月次スケジュール**
   - 時刻入力（HH:MM形式）
   - 日付選択（複数選択可、1～31）
   - 例: 「毎月 1, 15, 28日 09:00」

**複数スケジュール追加**:
- 「スケジュールを追加」ボタンで複数設定可能
- 各スケジュールは個別に削除可能

**期限設定**:
- 作成後の日数: 数値入力（0～365）
- 作成後の時間: 数値入力（0～8760）
- 両方未設定の場合: 期限なし

**実行期間**:
- 開始日: 日付選択、必須
- 終了日: 日付選択、任意（未設定 = 無期限）

**その他設定**:
- 祝日をスキップ: チェックボックス
- 祝日時は翌営業日に実行: チェックボックス
- 前回未完了タスクを削除: ❌ **常にtrue（ユーザーは変更不可）**
  - UIには表示しない
  - API送信時は自動的にtrue

**タグ**:
- カンマ区切りで複数入力可能
- 各タグ最大50文字

### 2.3 スケジュールタスク編集

#### 2.3.1 編集フロー

```
ScheduledTaskListScreen
    ↓
カードの「編集」ボタンタップ
    ↓
ScheduledTaskEditScreen表示
    ↓
既存データ表示（フォーム）
    ↓
内容変更
    ↓
「更新」ボタンタップ
    ↓
API呼び出し（PUT /api/scheduled-tasks/{id}）
    ↓
成功時にScheduledTaskListScreenへ戻る
```

**注意**: 作成画面と編集画面のフォーム構造は同じ（コンポーネント共通化推奨）

### 2.4 スケジュールタスク削除

#### 2.4.1 削除フロー

```
ScheduledTaskListScreen
    ↓
カードの「削除」ボタンタップ
    ↓
確認ダイアログ表示
「このスケジュールを削除しますか？」
    ↓
「削除」タップ
    ↓
API呼び出し（DELETE /api/scheduled-tasks/{id}）
    ↓
成功時に一覧から削除
```

### 2.5 スケジュールタスク一時停止・再開

#### 2.5.1 一時停止フロー

```
ScheduledTaskListScreen（有効状態のカード）
    ↓
「一時停止」ボタンタップ
    ↓
確認ダイアログ表示
「スケジュールを一時停止しますか？」
    ↓
「一時停止」タップ
    ↓
API呼び出し（POST /api/scheduled-tasks/{id}/pause）
    ↓
成功時にステータス更新（有効 → 一時停止）
```

#### 2.5.2 再開フロー

```
ScheduledTaskListScreen（一時停止状態のカード）
    ↓
「再開」ボタンタップ
    ↓
API呼び出し（POST /api/scheduled-tasks/{id}/resume）
    ↓
成功時にステータス更新（一時停止 → 有効）
```

### 2.6 実行履歴表示

#### 2.6.1 実行履歴一覧フロー

```
ScheduledTaskListScreen
    ↓
カードの「履歴」ボタンタップ
    ↓
ScheduledTaskHistoryScreen表示
    ↓
実行履歴一覧表示（テーブル形式）
```

**表示内容**:
- 実行日時
- 実行結果（成功・失敗・スキップ）
- 作成されたタスクID（成功時）
- 削除されたタスクID（前回未完了削除時）
- 備考（スキップ理由等）
- エラーメッセージ（失敗時）

**実行結果の表示**:
- ✅ **成功**: 緑色バッジ、「タスク作成完了」
- ❌ **失敗**: 赤色バッジ、「エラー: {エラーメッセージ}」
- ⏭️ **スキップ**: グレー色バッジ、「スキップ: {理由}」

---

## 3. 画面設計

### 3.1 ScheduledTaskListScreen（一覧画面）

**レイアウト（スケジュール存在時）**:
```
┌─────────────────────────────────┐
│ ← スケジュールタスク       [+]   │
├─────────────────────────────────┤
│                                 │
│ ┌───────────────────────────┐  │
│ │ ✅ 有効                    │  │
│ │ 毎週月曜日のゴミ出し        │  │
│ │                           │  │
│ │ 📅 毎週 月 09:00          │  │
│ │ 👤 山田太郎               │  │
│ │ 💰 100トークン            │  │
│ │                           │  │
│ │ [編集] [履歴] [一時停止]  │  │
│ └───────────────────────────┘  │
│                                 │
│ ┌───────────────────────────┐  │
│ │ ⏸️ 一時停止                │  │
│ │ 毎月1日の定期レポート       │  │
│ │                           │  │
│ │ 📅 毎月 1日 10:00         │  │
│ │ 👥 グループ全員           │  │
│ │ 💰 報酬なし               │  │
│ │                           │  │
│ │ [編集] [履歴] [再開] [削除]│  │
│ └───────────────────────────┘  │
│                                 │
└─────────────────────────────────┘
```

**レイアウト（空状態）**:
```
┌─────────────────────────────────┐
│ ← スケジュールタスク       [+]   │
├─────────────────────────────────┤
│                                 │
│         📅                      │
│   スケジュールタスクなし         │
│                                 │
│ 定期的に自動実行するタスクを    │
│ 設定できます。                   │
│                                 │
│    [スケジュールを作成]          │
│                                 │
└─────────────────────────────────┘
```

### 3.2 ScheduledTaskCreateScreen（作成画面）

**レイアウト**:
```
┌─────────────────────────────────┐
│ ← スケジュール作成               │
├─────────────────────────────────┤
│                                 │
│ 📋 基本情報                     │
│                                 │
│ タイトル *                      │
│ [________________________]      │
│                                 │
│ 説明                            │
│ [________________________]      │
│ [________________________]      │
│                                 │
│ ☑ 画像添付必須                   │
│ ☑ 承認必須                       │
│                                 │
│ 報酬トークン                     │
│ [100        ] トークン          │
│                                 │
│ ────────────────────────        │
│                                 │
│ 👥 担当者設定                    │
│                                 │
│ 担当者を指定                     │
│ [グループ全員  ▼]               │
│                                 │
│ ────────────────────────        │
│                                 │
│ 📅 スケジュール設定 *            │
│                                 │
│ ┌───────────────────────────┐  │
│ │ [日次▼] 時刻 [09:00]       │  │
│ │                      [削除]│  │
│ └───────────────────────────┘  │
│                                 │
│ [+ スケジュールを追加]           │
│                                 │
│ ────────────────────────        │
│                                 │
│ ⏱️ 期限設定                     │
│                                 │
│ 作成後の日数: [3  ] 日          │
│ 作成後の時間: [0  ] 時間        │
│                                 │
│ ────────────────────────        │
│                                 │
│ 📆 実行期間                     │
│                                 │
│ 開始日 *: [2025-12-09]          │
│ 終了日:   [2026-12-31]          │
│                                 │
│ ────────────────────────        │
│                                 │
│ ⚙️ その他設定                    │
│                                 │
│ ☑ 祝日をスキップ                 │
│ ☐ 祝日時は翌営業日に実行         │
│                                 │
│ ────────────────────────        │
│                                 │
│ 🏷️ タグ                         │
│                                 │
│ [家事, 掃除          ]          │
│                                 │
│ ────────────────────────        │
│                                 │
│      [キャンセル] [作成]        │
│                                 │
└─────────────────────────────────┘
```

### 3.3 ScheduledTaskEditScreen（編集画面）

**レイアウト**: 作成画面と同じフォーム構造

```
┌─────────────────────────────────┐
│ ← スケジュール編集               │
├─────────────────────────────────┤
│                                 │
│ （作成画面と同じフォーム）       │
│ （既存データが入力された状態）   │
│                                 │
│      [キャンセル] [更新]        │
│                                 │
└─────────────────────────────────┘
```

### 3.4 ScheduledTaskHistoryScreen（実行履歴画面）

**レイアウト**:
```
┌─────────────────────────────────┐
│ ← 実行履歴                       │
│   毎週月曜日のゴミ出し            │
├─────────────────────────────────┤
│                                 │
│ 📊 実行履歴（最新10件）          │
│                                 │
│ ┌───────────────────────────┐  │
│ │ 2025-12-09 09:00          │  │
│ │ ✅ 成功                    │  │
│ │ タスク3件作成完了          │  │
│ │ 作成: #123, #124, #125    │  │
│ └───────────────────────────┘  │
│                                 │
│ ┌───────────────────────────┐  │
│ │ 2025-12-02 09:00          │  │
│ │ ✅ 成功                    │  │
│ │ 前回未完了タスク2件削除    │  │
│ │ 削除: #120, #121          │  │
│ │ 新規タスク3件作成          │  │
│ │ 作成: #122, #123, #124    │  │
│ └───────────────────────────┘  │
│                                 │
│ ┌───────────────────────────┐  │
│ │ 2025-11-25 09:00          │  │
│ │ ⏭️ スキップ                │  │
│ │ 理由: 祝日のためスキップ   │  │
│ └───────────────────────────┘  │
│                                 │
│ ┌───────────────────────────┐  │
│ │ 2025-11-18 09:00          │  │
│ │ ❌ 失敗                    │  │
│ │ エラー: トークン不足       │  │
│ └───────────────────────────┘  │
│                                 │
└─────────────────────────────────┘
```

---

## 4. API仕様

### 4.1 スケジュールタスク一覧取得

**エンドポイント**: `GET /api/scheduled-tasks`

**クエリパラメータ**:
- `group_id` (optional): グループID

**レスポンス**:
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "group_id": 1,
      "title": "毎週月曜日のゴミ出し",
      "description": "可燃ゴミを出してください",
      "requires_image": false,
      "requires_approval": false,
      "reward": 100,
      "assigned_user_id": null,
      "auto_assign": false,
      "schedules": [
        {
          "type": "weekly",
          "time": "09:00",
          "days": [1]
        }
      ],
      "due_duration_days": 0,
      "due_duration_hours": 0,
      "start_date": "2025-12-09",
      "end_date": null,
      "skip_holidays": true,
      "move_to_next_business_day": false,
      "delete_incomplete_previous": true,
      "tags": ["家事", "掃除"],
      "is_active": true,
      "paused_at": null,
      "created_at": "2025-12-08T10:00:00Z",
      "updated_at": "2025-12-08T10:00:00Z"
    }
  ]
}
```

**実装状況**: ✅ 実装済み（`IndexScheduledTaskApiAction`）

### 4.2 スケジュールタスク作成フォーム取得

**エンドポイント**: `GET /api/scheduled-tasks/create`

**クエリパラメータ**:
- `group_id` (optional): グループID

**レスポンス**:
```json
{
  "success": true,
  "data": {
    "group_members": [
      {
        "id": 1,
        "name": "山田太郎",
        "username": "taro"
      },
      {
        "id": 2,
        "name": "山田花子",
        "username": "hanako"
      }
    ],
    "tags": ["家事", "掃除", "勉強"],
    "defaults": {
      "reward": 0,
      "requires_image": false,
      "requires_approval": false,
      "skip_holidays": false,
      "move_to_next_business_day": false,
      "delete_incomplete_previous": true,
      "start_date": "2025-12-09"
    }
  }
}
```

**実装状況**: ✅ 実装済み（`CreateScheduledTaskApiAction`）

### 4.3 スケジュールタスク作成

**エンドポイント**: `POST /api/scheduled-tasks`

**リクエスト**:
```json
{
  "title": "毎週月曜日のゴミ出し",
  "description": "可燃ゴミを出してください",
  "requires_image": false,
  "requires_approval": false,
  "reward": 100,
  "assigned_user_id": null,
  "schedules": [
    {
      "type": "weekly",
      "time": "09:00",
      "days": [1]
    }
  ],
  "due_duration_days": 0,
  "due_duration_hours": 0,
  "start_date": "2025-12-09",
  "end_date": null,
  "skip_holidays": true,
  "move_to_next_business_day": false,
  "tags": ["家事", "掃除"]
}
```

**注意**: `delete_incomplete_previous`は常にtrueで固定（送信不要）

**レスポンス**:
```json
{
  "success": true,
  "message": "スケジュールタスクを作成しました。",
  "data": {
    "id": 1,
    "title": "毎週月曜日のゴミ出し"
  }
}
```

**実装状況**: ✅ 実装済み（`StoreScheduledTaskApiAction`）

### 4.4 スケジュールタスク編集フォーム取得

**エンドポイント**: `GET /api/scheduled-tasks/{id}/edit`

**レスポンス**:
```json
{
  "success": true,
  "data": {
    "scheduled_task": {
      "id": 1,
      "title": "毎週月曜日のゴミ出し",
      "description": "可燃ゴミを出してください",
      "requires_image": false,
      "requires_approval": false,
      "reward": 100,
      "assigned_user_id": null,
      "schedules": [
        {
          "type": "weekly",
          "time": "09:00",
          "days": [1]
        }
      ],
      "due_duration_days": 0,
      "due_duration_hours": 0,
      "start_date": "2025-12-09",
      "end_date": null,
      "skip_holidays": true,
      "move_to_next_business_day": false,
      "tags": ["家事", "掃除"]
    },
    "group_members": [
      {
        "id": 1,
        "name": "山田太郎"
      }
    ]
  }
}
```

**実装状況**: ✅ 実装済み（`EditScheduledTaskApiAction`）

### 4.5 スケジュールタスク更新

**エンドポイント**: `PUT /api/scheduled-tasks/{id}`

**リクエスト**: 作成時と同じフォーマット

**レスポンス**:
```json
{
  "success": true,
  "message": "スケジュールタスクを更新しました。"
}
```

**実装状況**: ✅ 実装済み（`UpdateScheduledTaskApiAction`）

### 4.6 スケジュールタスク削除

**エンドポイント**: `DELETE /api/scheduled-tasks/{id}`

**レスポンス**:
```json
{
  "success": true,
  "message": "スケジュールタスクを削除しました。"
}
```

**実装状況**: ✅ 実装済み（`DeleteScheduledTaskApiAction`）

### 4.7 スケジュールタスク一時停止

**エンドポイント**: `POST /api/scheduled-tasks/{id}/pause`

**レスポンス**:
```json
{
  "success": true,
  "message": "スケジュールタスクを一時停止しました。"
}
```

**実装状況**: ✅ 実装済み（`PauseScheduledTaskApiAction`）

### 4.8 スケジュールタスク再開

**エンドポイント**: `POST /api/scheduled-tasks/{id}/resume`

**レスポンス**:
```json
{
  "success": true,
  "message": "スケジュールタスクを再開しました。"
}
```

**実装状況**: ✅ 実装済み（`ResumeScheduledTaskApiAction`）

### 4.9 実行履歴取得

**エンドポイント**: `GET /api/scheduled-tasks/{id}/history`

**レスポンス**:
```json
{
  "success": true,
  "data": {
    "scheduled_task": {
      "id": 1,
      "title": "毎週月曜日のゴミ出し"
    },
    "executions": [
      {
        "id": 1,
        "scheduled_task_id": 1,
        "created_task_id": 123,
        "deleted_task_id": 120,
        "executed_at": "2025-12-09T09:00:00Z",
        "status": "success",
        "note": "タスク3件作成、前回未完了2件削除",
        "error_message": null
      },
      {
        "id": 2,
        "scheduled_task_id": 1,
        "created_task_id": null,
        "deleted_task_id": null,
        "executed_at": "2025-11-25T09:00:00Z",
        "status": "skipped",
        "note": "祝日のためスキップ",
        "error_message": null
      },
      {
        "id": 3,
        "scheduled_task_id": 1,
        "created_task_id": null,
        "deleted_task_id": null,
        "executed_at": "2025-11-18T09:00:00Z",
        "status": "failed",
        "note": null,
        "error_message": "トークン不足: グループマスターのトークン残高が不足しています"
      }
    ]
  }
}
```

**実装状況**: ❌ **未実装** - Phase 2.B-7で実装が必要

---

## 5. データ設計

### 5.1 型定義（scheduled-task.types.ts）

```typescript
/**
 * スケジュール種別
 */
export type ScheduleType = 'daily' | 'weekly' | 'monthly';

/**
 * スケジュール設定
 */
export interface Schedule {
  type: ScheduleType;
  time: string; // HH:MM形式
  days?: number[]; // 週次の場合（0=日曜, 1=月曜, ..., 6=土曜）
  dates?: number[]; // 月次の場合（1～31）
}

/**
 * スケジュールタスク基本情報
 */
export interface ScheduledTask {
  id: number;
  group_id: number;
  created_by: number;
  title: string;
  description: string | null;
  requires_image: boolean;
  reward: number;
  requires_approval: boolean;
  assigned_user_id: number | null;
  auto_assign: boolean;
  schedules: Schedule[];
  due_duration_days: number | null;
  due_duration_hours: number | null;
  start_date: string; // YYYY-MM-DD
  end_date: string | null; // YYYY-MM-DD
  skip_holidays: boolean;
  move_to_next_business_day: boolean;
  delete_incomplete_previous: boolean;
  tags: string[];
  is_active: boolean;
  paused_at: string | null;
  created_at: string;
  updated_at: string;
}

/**
 * 実行履歴ステータス
 */
export type ExecutionStatus = 'success' | 'failed' | 'skipped';

/**
 * 実行履歴
 */
export interface ScheduledTaskExecution {
  id: number;
  scheduled_task_id: number;
  created_task_id: number | null;
  deleted_task_id: number | null;
  executed_at: string;
  status: ExecutionStatus;
  note: string | null;
  error_message: string | null;
}

/**
 * スケジュールタスク作成・更新リクエスト
 */
export interface ScheduledTaskRequest {
  title: string;
  description?: string;
  requires_image?: boolean;
  reward?: number;
  requires_approval?: boolean;
  assigned_user_id?: number | null;
  schedules: Schedule[];
  due_duration_days?: number | null;
  due_duration_hours?: number | null;
  start_date: string;
  end_date?: string | null;
  skip_holidays?: boolean;
  move_to_next_business_day?: boolean;
  tags?: string[];
}

/**
 * スケジュールタスク作成フォームデータ
 */
export interface ScheduledTaskFormData {
  group_members: Array<{
    id: number;
    name: string;
    username: string;
  }>;
  tags: string[];
  defaults: {
    reward: number;
    requires_image: boolean;
    requires_approval: boolean;
    skip_holidays: boolean;
    move_to_next_business_day: boolean;
    delete_incomplete_previous: boolean;
    start_date: string;
  };
}

/**
 * 実行履歴レスポンス
 */
export interface ScheduledTaskHistoryResponse {
  scheduled_task: {
    id: number;
    title: string;
  };
  executions: ScheduledTaskExecution[];
}

/**
 * API共通レスポンス
 */
export interface ScheduledTaskApiResponse {
  success: boolean;
  message: string;
  data?: {
    id?: number;
    title?: string;
  };
}
```

---

## 6. Service・Hook層設計

### 6.1 ScheduledTaskService（scheduled-task.service.ts）

```typescript
/**
 * スケジュールタスク管理サービス
 */
export const scheduledTaskService = {
  /**
   * スケジュールタスク一覧取得
   */
  async getScheduledTasks(groupId?: number): Promise<ScheduledTask[]> { /* ... */ },
  
  /**
   * スケジュールタスク作成フォーム取得
   */
  async getCreateFormData(groupId?: number): Promise<ScheduledTaskFormData> { /* ... */ },
  
  /**
   * スケジュールタスク作成
   */
  async createScheduledTask(data: ScheduledTaskRequest): Promise<void> { /* ... */ },
  
  /**
   * スケジュールタスク編集フォーム取得
   */
  async getEditFormData(id: number): Promise<{ scheduled_task: ScheduledTask; group_members: any[] }> { /* ... */ },
  
  /**
   * スケジュールタスク更新
   */
  async updateScheduledTask(id: number, data: ScheduledTaskRequest): Promise<void> { /* ... */ },
  
  /**
   * スケジュールタスク削除
   */
  async deleteScheduledTask(id: number): Promise<void> { /* ... */ },
  
  /**
   * スケジュールタスク一時停止
   */
  async pauseScheduledTask(id: number): Promise<void> { /* ... */ },
  
  /**
   * スケジュールタスク再開
   */
  async resumeScheduledTask(id: number): Promise<void> { /* ... */ },
  
  /**
   * 実行履歴取得
   */
  async getExecutionHistory(id: number): Promise<ScheduledTaskHistoryResponse> { /* ... */ },
};
```

### 6.2 useScheduledTasks Hook（useScheduledTasks.ts）

```typescript
/**
 * スケジュールタスク管理Hook
 */
export const useScheduledTasks = () => {
  const [scheduledTasks, setScheduledTasks] = useState<ScheduledTask[]>([]);
  const [formData, setFormData] = useState<ScheduledTaskFormData | null>(null);
  const [executionHistory, setExecutionHistory] = useState<ScheduledTaskHistoryResponse | null>(null);
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  
  /**
   * スケジュールタスク一覧取得
   */
  const getScheduledTasks = async (groupId?: number) => { /* ... */ };
  
  /**
   * スケジュールタスク作成フォーム取得
   */
  const getCreateFormData = async (groupId?: number) => { /* ... */ };
  
  /**
   * スケジュールタスク作成
   */
  const createScheduledTask = async (data: ScheduledTaskRequest) => { /* ... */ };
  
  /**
   * スケジュールタスク編集フォーム取得
   */
  const getEditFormData = async (id: number) => { /* ... */ };
  
  /**
   * スケジュールタスク更新
   */
  const updateScheduledTask = async (id: number, data: ScheduledTaskRequest) => { /* ... */ };
  
  /**
   * スケジュールタスク削除
   */
  const deleteScheduledTask = async (id: number) => { /* ... */ };
  
  /**
   * スケジュールタスク一時停止
   */
  const pauseScheduledTask = async (id: number) => { /* ... */ };
  
  /**
   * スケジュールタスク再開
   */
  const resumeScheduledTask = async (id: number) => { /* ... */ };
  
  /**
   * 実行履歴取得
   */
  const getExecutionHistory = async (id: number) => { /* ... */ };
  
  return {
    scheduledTasks,
    formData,
    executionHistory,
    isLoading,
    error,
    getScheduledTasks,
    getCreateFormData,
    createScheduledTask,
    getEditFormData,
    updateScheduledTask,
    deleteScheduledTask,
    pauseScheduledTask,
    resumeScheduledTask,
    getExecutionHistory,
  };
};
```

---

## 7. テスト要件

### 7.1 Serviceテスト（scheduled-task.service.test.ts）

- ✅ スケジュールタスク一覧取得成功
- ✅ スケジュールタスク作成フォーム取得成功
- ✅ スケジュールタスク作成成功
- ✅ スケジュールタスク編集フォーム取得成功
- ✅ スケジュールタスク更新成功
- ✅ スケジュールタスク削除成功
- ✅ スケジュールタスク一時停止成功
- ✅ スケジュールタスク再開成功
- ✅ 実行履歴取得成功
- ✅ APIエラーハンドリング
- ✅ ネットワークエラーハンドリング

**目標**: 11テスト、全パス

### 7.2 Hookテスト（useScheduledTasks.test.ts）

- ✅ スケジュールタスク一覧取得（成功・失敗）
- ✅ スケジュールタスク作成フォーム取得（成功・失敗）
- ✅ スケジュールタスク作成（成功・失敗）
- ✅ スケジュールタスク編集フォーム取得（成功・失敗）
- ✅ スケジュールタスク更新（成功・失敗）
- ✅ スケジュールタスク削除（成功・失敗）
- ✅ スケジュールタスク一時停止（成功・失敗）
- ✅ スケジュールタスク再開（成功・失敗）
- ✅ 実行履歴取得（成功・失敗）
- ✅ ローディング状態管理
- ✅ エラー状態管理

**目標**: 20テスト、全パス

### 7.3 UIテスト

**ScheduledTaskListScreen.test.tsx**:
- ✅ スケジュールタスク一覧表示
- ✅ 空状態表示
- ✅ スケジュールタスク作成画面へ遷移
- ✅ スケジュールタスク編集画面へ遷移
- ✅ スケジュールタスク削除確認
- ✅ スケジュールタスク一時停止
- ✅ スケジュールタスク再開
- ✅ 実行履歴画面へ遷移

**ScheduledTaskCreateScreen.test.tsx**:
- ✅ フォーム表示
- ✅ 基本情報入力
- ✅ スケジュール追加
- ✅ スケジュール削除
- ✅ タグ入力
- ✅ バリデーションエラー表示
- ✅ スケジュールタスク作成成功

**ScheduledTaskEditScreen.test.tsx**:
- ✅ 既存データ表示
- ✅ スケジュールタスク更新成功

**ScheduledTaskHistoryScreen.test.tsx**:
- ✅ 実行履歴表示
- ✅ 実行結果バッジ表示（成功・失敗・スキップ）

**目標**: 20テスト、全パス

---

## 8. 実装優先順位

### Phase 2.B-7 Week 1（スケジュールタスク機能、5日間）

**Day 1: Laravel API確認・型定義作成**
- Laravel API動作確認（8エンドポイント）
- 実行履歴API実装（`GetScheduledTaskHistoryApiAction`）
- `scheduled-task.types.ts` 作成（15型定義）

**Day 2: Service層実装**
- `scheduled-task.service.ts` 実装（9メソッド）
- `scheduled-task.service.test.ts` 作成（11テスト）

**Day 3: Hook層実装**
- `useScheduledTasks.ts` 実装（9メソッド、280行目標）
- `useScheduledTasks.test.ts` 作成（20テスト）

**Day 4: UI実装（一覧・履歴）**
- `ScheduledTaskListScreen.tsx` 実装（550行目標）
- `ScheduledTaskHistoryScreen.tsx` 実装（380行目標）
- UIテスト作成（10テスト）

**Day 5: UI実装（作成・編集）**
- `ScheduledTaskCreateScreen.tsx` 実装（750行目標）
- `ScheduledTaskEditScreen.tsx` 実装（680行目標）
- UIテスト作成（10テスト）
- 動作確認・バグ修正

---

## 9. Laravel API実装タスク

### 9.1 実行履歴API実装

**ファイル**: `app/Http/Actions/Api/ScheduledTask/GetScheduledTaskHistoryApiAction.php`

```php
<?php

namespace App\Http\Actions\Api\ScheduledTask;

use App\Models\ScheduledGroupTask;
use App\Http\Responders\Api\ScheduledTask\ScheduledTaskApiResponder;
use Illuminate\Http\JsonResponse;

class GetScheduledTaskHistoryApiAction
{
    public function __construct(
        protected ScheduledTaskApiResponder $responder
    ) {}

    public function __invoke(int $id): JsonResponse
    {
        $scheduledTask = ScheduledGroupTask::findOrFail($id);
        
        // 権限チェック（グループメンバーのみ）
        // 実行履歴取得（最新50件）
        
        return $this->responder->history($scheduledTask, $executions);
    }
}
```

**ルート追加**:
```php
// routes/api.php
Route::get('/{id}/history', GetScheduledTaskHistoryApiAction::class)
    ->name('api.scheduled-tasks.history');
```

---

## 10. 参考資料

- **Web版スケジュールタスク一覧**: `/home/ktr/mtdev/resources/views/batch/index.blade.php`
- **Web版スケジュールタスク作成・編集フォーム**: `/home/ktr/mtdev/resources/views/batch/partials/scheduled-task-form-edit.blade.php`
- **Web版実行履歴画面**: `/home/ktr/mtdev/resources/views/batch/history.blade.php`
- **要件定義書（Web版）**: `/home/ktr/mtdev/definitions/ScheduleGroupTask.md`
- **Laravel API実装**: `/home/ktr/mtdev/app/Http/Actions/Api/ScheduledTask/`
- **モバイル開発規則**: `/home/ktr/mtdev/docs/mobile/mobile-rules.md`
- **プロジェクト規約**: `/home/ktr/mtdev/.github/copilot-instructions.md`
